---
layout: main.njk
---

<div class="home">
	<div class="card mb-2">
		<div class="card-body">
		Dev Radar is an AI-powered news aggregator that helps you stay up to date with the latest trends in software development. <br/> Below are the latest articles that our AI 🤖 has found for you.
		</div>
	</div>

	<div class="row">

		{% for topic in topics %}

			<div class="col-sm-4">

				<div class="card mb-2 topic">

					<div class="card-header">
						<h3>{{topic.name}}</h3>
					</div>
					<div class="card-body">

						<div class="list-group list-group-flush">
							{% for article in topic.articles %}

								<div class="list-group-item">
									<a  href="{{ article.url }}">
										<div>{{ article.title }}</div>
									</a>

									<small>
										{{ article.formattedDate }}
									</small>

									<div>{{article.article.slice(0,200)}}...</div>

								</div>
							{% endfor %}

						</div>

					</div>

					<div class="card-footer">
						<a href="/topics/{{topic.slug}}">All {{topic.name}} Articles</a>
					</div>

				</div>

			</div>

		{% endfor %}

	</div>
	<div class="card">

		<div class="card-header">
			<h2 class="mb-0">Latest Articles</h2>
		</div>
		<div class="card-body">
			<div class="list-group list-group-flush">
				{% for article in latest %}

					<a class="list-group-item" href="{{ article.url }}">
						{{ article.title }} - 
						<small>{{ article.formattedDate }}</small>
					</a>
				{% endfor %}
			</div>
		</div>
	</div>
</div>