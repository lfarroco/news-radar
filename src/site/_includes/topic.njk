---
layout: main.njk
---

<div>
	<h1>{{ title }}</h1>
	<ul>
		{% for article in articles %}
			<li>

				<a href="{{ article.url }}">{{ article.title }}</a>
				{{ article.formattedDate }}
			</li>
		{% endfor %}

	</ul>
</div>